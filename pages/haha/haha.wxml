<!--index.wxml-->
<view>
    <view class='imgList'>
        <image class='img' wx:for='{{imgArr}}' wx:key="{{imgArr.index}}" src='{{item}}' data-index='{{index}}' data-imglist="{{imgArr}}" bindtap='previewImg'></image>
    </view>
    <view>
        <image class='image' wx:for='{{imgList}}' wx:key='{{imgList.index}}' src='{{item}}' data-index='{{index}}' data-imglist="{{imgList}}" bindtap='previewImg'></image>
        <!-- <image src="../../common/images/hdImg.jpg" class="bgi"></image> -->
    </view>
</view>